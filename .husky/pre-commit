if [ -f .git/MERGE_HEAD ]; then
  echo "Merge detected. Skipping lint-staged and generators."
  exit 0
fi

corepack yarn lint-staged --verbose
corepack yarn app-store:build && git add packages/app-store/*.generated.*
